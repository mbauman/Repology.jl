{
    "name": "GeneralMetadata.jl",
    "dockerComposeFile": "docker-compose.yml",
    "service": "app",
    "workspaceFolder": "/workspaces/GeneralMetadata.jl",
    "postCreateCommand": "sudo install -dm 755 /etc/apt/keyrings && curl -s https://www.postgresql.org/media/keys/ACCC4CF8.asc | sudo gpg --dearmor -o /etc/apt/keyrings/pgdg.gpg && sudo sh -c 'echo \"deb [signed-by=/etc/apt/keyrings/pgdg.gpg] http://apt.postgresql.org/pub/repos/apt $(lsb_release -cs)-pgdg main\" > /etc/apt/sources.list.d/pgdg.list' && sudo apt-get update && sudo apt-get install -y postgresql-client-17",
    "remoteEnv": {
        "PGHOST": "db",
        "PGPORT": "5432",
        "PGUSER": "repology",
        "PGPASSWORD": "repology",
        "PGDATABASE": "repology"
    }
}
